# Notepad | Nest.js API

## Overview

This is a Nest.js application created for a Tecnical Test for ENSOLVERS.

## Start the service

## Pre-requisites
Is required to have Docker and Docker Compose installed on the host computer.

## Run the project

To run the project isolated open a terminal on /backend directory and then run:
```bash
cp ./.env.example ./.env
docker-compose up
```

Author: Miguel Benjamin Atencio Vargas
